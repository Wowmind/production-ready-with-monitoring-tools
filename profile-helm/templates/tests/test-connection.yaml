apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "profile-helm.fullname" . }}-test-connection"
  labels:
    {{- include "profile-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "profile-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
