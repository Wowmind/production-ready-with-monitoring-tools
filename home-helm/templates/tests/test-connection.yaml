apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "home-helm.fullname" . }}-test-connection"
  labels:
    {{- include "home-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "home-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
